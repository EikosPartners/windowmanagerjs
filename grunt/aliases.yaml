lint:
    - 'jshint:extension'
    - 'jsdoc'

compile:
    - 'lint'

config:
    - 'bowerRequirejs'

build:
    - 'compile'
    - 'config'
    - 'requirejs'
    - 'uglify'
    - 'copy:build'

release:
    - 'build'
    - 'copy:release'

test:
    - 'build'
    - 'if-missing:curl-dir:jasmine'
    - 'connect:test'

