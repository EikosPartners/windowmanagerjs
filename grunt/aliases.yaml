lint:
    - 'jshint:extension'
    - 'version'
    - 'jsdoc'

compile:
    - 'lint'

config:
    - 'bowerRequirejs'

build:
    - 'compile'
    - 'concat'
    - 'babel'
    - 'copy:build'

release:
    - 'build'
    - 'uglify'
    - 'copy:release'

test:
    - 'build'
    - 'if-missing:curl-dir:jasmine'
    - 'connect:test'

