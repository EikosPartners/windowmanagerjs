lint:
    - 'jshint:extension'
    - 'version'
    - 'jsdoc'

compile:
    - 'lint'

build:
    - 'compile'
    - 'concat'
    - 'babel'

release:
    - 'build'
    - 'uglify'

test:
    - 'build'
    - 'if-missing:curl-dir:jasmine'
    - 'connect:test'

